[build]
dockerfilePath = "kitz-notifications-queue/Dockerfile"

[deploy]
startCommand = "npx tsx src/index.ts"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
