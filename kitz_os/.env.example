# ─── KITZ OS Environment ───────────────────────────────
PORT=3012
NODE_ENV=development

# REQUIRED secrets — generate with: openssl rand -hex 32
DEV_TOKEN_SECRET=
JWT_SECRET=
SERVICE_SECRET=

# ─── AI: Hybrid Load-Balanced Strategy ──────────────────
#
# THINKING (Claude preferred → OpenAI fallback):
#   Opus/gpt-4o    → strategy, C-suite (high-value, impactful)
#   Sonnet/gpt-4o  → analysis, content, planning (mid-tier)
#   Haiku/gpt-4o-mini → classification, extraction, voice (fast)
#
# EXECUTING (OpenAI preferred → Claude fallback):
#   gpt-4o-mini/Haiku → tool-routing loops (cheapest for volume)
#
# TRANSCRIPTION (OpenAI only — no Claude equivalent):
#   Whisper → voice-to-text
#
CLAUDE_API_KEY=
ANTHROPIC_API_KEY=

# OpenAI: transactional tool-routing + Whisper transcription + DALL-E image generation
# Set EITHER AI_API_KEY or OPENAI_API_KEY (both work, AI_API_KEY takes priority)
# Key must start with "sk-" — get one at https://platform.openai.com/api-keys
AI_API_KEY=
OPENAI_API_KEY=
AI_MODEL=gpt-4o-mini

# ─── Supabase (kitz workspace / governance) ─────────────
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ─── Business ─────────────────────────────────────────
BUSINESS_ID=

# ─── Workspace MCP (CRM, orders, storefronts) ──────────
WORKSPACE_MCP_URL=
WORKSPACE_MCP_KEY=
GOD_MODE_USER_ID=

# ─── WhatsApp Connector ──────────────────────────────
WA_CONNECTOR_URL=http://localhost:3006
