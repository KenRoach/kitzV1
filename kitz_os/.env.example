# ─── KITZ OS Environment ───────────────────────────────
PORT=3012
DEV_TOKEN_SECRET=dev-secret-change-me
NODE_ENV=development

# ─── AI: Hybrid Load-Balanced Strategy ──────────────────
#
# THINKING (Claude preferred → OpenAI fallback):
#   Opus/gpt-4o    → strategy, C-suite (high-value, impactful)
#   Sonnet/gpt-4o  → analysis, content, planning (mid-tier)
#   Haiku/gpt-4o-mini → classification, extraction, voice (fast)
#
# EXECUTING (OpenAI preferred → Claude fallback):
#   gpt-4o-mini/Haiku → tool-routing loops (cheapest for volume)
#
# TRANSCRIPTION (OpenAI only — no Claude equivalent):
#   Whisper → voice-to-text
#
CLAUDE_API_KEY=
ANTHROPIC_API_KEY=

# OpenAI: transactional tool-routing + Whisper transcription + fallback
AI_API_KEY=
AI_MODEL=gpt-4o-mini

# ─── Supabase (kitz workspace / governance) ─────────────
SUPABASE_URL=https://yjustozltqpsroxkjobf.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ─── Business ─────────────────────────────────────────
BUSINESS_ID=134f52d0-90a3-4806-a1bd-c37ef31d4f6f

# ─── xyz88-io MCP (CRM, orders, storefronts) ──────────
XYZ88_MCP_URL=https://mqkjbejuuedauygeccbj.supabase.co/functions/v1/mcp-server/mcp
XYZ88_MCP_KEY=
GOD_MODE_USER_ID=8787fee9-d06a-442f-91ba-fd082b134ccf

# ─── WhatsApp Connector ──────────────────────────────
WA_CONNECTOR_URL=http://localhost:3006
