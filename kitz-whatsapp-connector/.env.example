PORT=3006
TRACE_HEADER=x-trace-id

# Auth — must match DEV_TOKEN_SECRET / SERVICE_SECRET in kitz-os
SERVICE_SECRET=dev-secret-change-me
DEV_TOKEN_SECRET=dev-secret-change-me

# KITZ OS URL — where inbound WhatsApp messages are forwarded for AI processing
KITZ_OS_URL=http://localhost:3012
